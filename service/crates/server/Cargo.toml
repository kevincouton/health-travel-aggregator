[package]
name = "server"
version = "0.1.0"
edition = "2021"

[lib]
name = "server"
path = "src/lib.rs"

[[bin]]
name = "server"
path = "src/main.rs"

[dependencies]
chassis = { path = "../chassis" }
axum = { workspace = true }
tower = { workspace = true }
tower-http = { workspace = true }
sqlx = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
anyhow = { workspace = true }
async-trait = { workspace = true }
# default-features off: the default "transport" pulls native-tls; this stack
# standardizes on rustls (see chassis R-3). Adds reqwest 0.12 alongside the
# pinned 0.11 — build-time cost only.
sentry = { version = "0.49.2", default-features = false, features = ["backtrace", "contexts", "panic", "release-health", "reqwest", "rustls"] }
sentry-tracing = "0.49.2"

[dev-dependencies]
tokio = { workspace = true }
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
